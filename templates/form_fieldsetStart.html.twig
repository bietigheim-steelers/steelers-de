<fieldset{{ attrs().addClass(['border-b border-gray-900/10', this.class|default]) }}>
    {% if this.label %}
        <legend class="text-2xl text-black font-sans uppercase font-bold !mt-0 mb-4">{{ this.label|insert_tag_raw }}</legend>
    {% endif %}

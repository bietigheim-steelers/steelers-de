{% extends '@Contao/content_element/_base.html.twig' %}
{% use '@Contao/component/_figure.html.twig' %}

{% block content %}
  <div class="bg-steelgreen flex justify-center">
    <div class="z-10">

      {% if images %}
        {% with {figure: images|first} %}
        {{ block('figure_component') }}
        {% endwith %}
      {% endif %}
    </div>
  </div>
{% endblock %}

<div class="flex items-center justify-center h-screen">
  <div>
    <h2 class="block-headline">{{ keys.code }}</h2>
    <h3 class="text-center text-5xl">{{ keys.title }}</h3>
    <p class="text-center mt-2 text-lg font-bold uppercase">{{ keys.desc }}</p>
    <p class="text-center mt-16">
      <a href="/" class="btn-default">Zur Startseite</a>
    </p>
  </div>
</div>
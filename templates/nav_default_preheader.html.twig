{% for item in items %}
    <div class="content-hyperlink block">
        <a{{ attrs()
            .set('href', item.href ?: './')
            .addClass('hyperlink_txt')
            .set('title', item.pageTitle ?: item.title)
        }}>{{ item.link }}</a>
    </div>
{% endfor %}

{% for item in items %}
    <div class="ce_hyperlink block">
        <a{{ attrs()
            .set('href', item.href ?: './')
            .addClass('hyperlink_txt')
            .set('title', item.pageTitle ?: item.title)
        }}>{{ item.link }}</a>
    </div>
{% endfor %}
